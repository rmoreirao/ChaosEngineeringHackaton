apiVersion: v1
kind: Service
metadata:
  name: postgres-exporter
  namespace: oranje-markt
  labels:
    app: postgres-exporter
spec:
  type: ClusterIP
  ports:
    - port: 9187
      targetPort: 9187
  selector:
    app: postgres-exporter
