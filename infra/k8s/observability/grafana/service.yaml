apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: oranje-markt
  labels:
    app: grafana
spec:
  type: ClusterIP
  ports:
    - port: 3001
      targetPort: 3000
  selector:
    app: grafana
